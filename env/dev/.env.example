# ===== ThreadBrief dev environment (example) =====
# Copy to env/dev/.env and edit values

# Web
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_GA_ID=

# API
APP_ENV=dev
CORS_ORIGINS=http://localhost:3000

# Used to build share links (e.g., /b/{id})
WEB_BASE_URL=http://localhost:3000

# Queue backend for Phase 0 local dev
QUEUE_BACKEND=redis
REDIS_URL=redis://redis:6379/0

# Optional: enable real Gemini generation
GEMINI_API_KEY=
GEMINI_ENDPOINT=https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent

# Optional: pass extra yt-dlp args (e.g. JS runtime)
YTDLP_ARGS=--js-runtimes node

# Optional: storage mode (default: memory)
STORAGE_BACKEND=memory

# Rate limit
RATE_LIMIT_PER_DAY=100

# Max video minutes (API enforcement)
MAX_VIDEO_MINUTES=180
